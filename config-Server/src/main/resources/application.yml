server:
  port: 7777

spring:
  profiles:
    active: default
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/galvacastell/Java-tarea02.git
          default-label: main
          clone-on-start: true
